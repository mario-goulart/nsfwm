(define csc-args
  (if (get-environment-variable "CWM_DEBUG")
      '(-d2 -O0 -S)
      '(-d0 -O3)))

(compile ,@csc-args -J -s cwm.scm)
(compile ,@csc-args -s cwm.import.scm)

(install-extension
 'cwm
 '("cwm.so" "cwm.import.so")
 '((version "0.0.1")))
